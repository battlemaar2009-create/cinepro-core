{
    "name": "@cinepro/core",
    "version": "1.0.0",
  "type": "module",
    "type": "module",
    "scripts": {
        "dev": "tsx watch src/server.ts",
        "build": "npm run build "build": "npm run build "build": "tsc --noEmit || echo Building...""build": "tsc --noEmit || echo Building..." cp dist/server.js server.js""build": "npm run build "build": "tsc --noEmit || echo Building...""build": "tsc --noEmit || echo Building..." cp dist/server.js server.js" cp dist/server.js server.js",
        "start": "npm run build && node dist/server.js",
        "format": "prettier --write src",
        "clean": "npx rimraf dist"
    },
    "dependencies": {
        "@omss/framework": "^1.1.19",
        "cheerio": "^1.2.0",
        "crypto-js": "^4.2.0",
        "dotenv": "^16.4.5"
    },
    "devDependencies": {
        "@types/crypto-js": "^4.2.2",
        "@types/node": "^22.7.4",
        "prettier": "^3.3.3",
        "tsx": "^4.19.1",
        "typescript": "^5.6.2"
    }
}
